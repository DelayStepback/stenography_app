name: stenography_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localization: ^0.2.2
  go_router: ^14.3.0
  intl: ^0.19.0
  logger: ^2.4.0
  provider: ^6.1.2
  image_picker: ^1.1.2
  # path_provider: 2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/icons/
